export class Voce {
  naziv: string;
  tezina: number;
  zapremina: number;

  constructor (naziv: string, tezina: number, zapremina: number) {
    this.naziv = naziv;
    this.tezina = tezina;
    this.zapremina = zapremina;
  }
}